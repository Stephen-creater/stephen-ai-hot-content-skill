---
name: stephen-ai-hot-content-skill
description: 按 Stephen 的历史文章与人工审核反馈，持续发现、全文核验并筛选适合日课二创的简体中文 AI 选题。用于寻找新选题、生成候选审核页、导入反馈和迭代编辑判断；不负责撰写文章正文。
---

# Stephen AI 热点选题

本 Skill 是编辑判断系统，不是关键词过滤器。自动程序负责发现、资格检查、风险提示和去重；“值得写、读者有收获、能够独立二创”必须依据完整正文进行人工终审。

## 高优先级：三个任务分渠道协作，一个任务统一交付

全网、多渠道选题优先采用 **3 个独立长期 session（任务）**，每个任务认领渠道，其中一个兼任统筹；总共三个，不是三个搜索任务再加一个统筹。单篇核验、反馈导入、局部改稿不机械启动三路。优先复用用户已授权参与的任务；先核对任务身份、当前状态和工具能力，不凭旧标题猜 ID，不把临时子 Agent 冒称独立长期任务。需要新建用户可见任务时，仅在用户明确要求创建后执行；暂时不足三个或通信不可用时，用现有任务推进并说明缺口，不伪称已并行。

**结果目标：每个 session 负责一组渠道，各自至少交付 2 条最终复核通过的新材料，整批至少 6 条。** 这里的三组不是只搜索三个网站；一组可以沿作者和引用链覆盖多个相关平台。不能用某组多交抵消另一组未完成。未达到各组目标时持续搜索、深读、筛选和替换，不设搜索轮数、阅读篇数、运行时长或探索深度上限；“做测试”默认也要拿到这个结果，不再擅自改成有限样本连通测试。若用户另行明确只测连通性，才使用有界检查。

每个任务保持一个可续跑的目标与检查点；使用可用的 Goal/持续执行机制，结束条件是合格材料与交付验收完成，不是读过若干篇。工具中断后恢复已保存进度；需要跨回合自动续跑时使用产品提供的接续机制，仅在任务空闲且目标未完成时续接，不重复启动正在运行的任务。找到零条只是中间结果，不生成空白审核页作为本轮交付。不可承诺一定存在足量供给，也不得为了达到数字而伪造、降低标准或绕过权限；真实授权阻塞明确报告，其他独立工作继续。

### 开始前认领，按结果调整

统筹先让三方读取同一版标准与最新反馈，确定本轮题目范围、唯一批次归属和交付入口。在 `.local/work/<本轮标识>/` 保存一份由统筹单独维护的协作记录：任务 ID、角色、认领渠道/作者范围、独占 Ego space、各自证据文件路径、进度和下一步。搜索任务只写自己的材料文件，通过消息请求调整认领，避免并发覆盖。

以下是起步分工，不是固定平台配额；依实际可读性、合格材料产出和工作量调整，统筹也承担一组渠道：

| 任务 | 起步渠道 | 额外责任 |
|---|---|---|
| A（兼统筹） | 公众号、中文独立博客、中文媒体与 RSS | 统一去重、全文复核、排序、发布与反馈导入 |
| B | 知乎、小红书、B站、中文播客 | 追踪作者与完整文章/逐字稿 |
| C | X、Reddit、Hacker News、YouTube、海外 newsletter、GitHub | 发现高价值问题与作者，再追中文成熟材料；英文只作核验 |

一个渠道或作者范围同一时刻只设一个主要负责人。认领完成后各自独立深挖，普通重复直接查共享历史并跳过，不逐条互发消息；仅当实际归属冲突影响交付时由统筹裁决。某路受阻或产出低，先在组内换作者、查询、入口和引用链继续；确需转交渠道时才联系统筹。不降低筛选标准，也不假定三个任务天然覆盖了全网。

### 先各自筛，再共同验

各任务取得完整正文后，按本 Skill 的资格门槛与五维标准先筛。交给统筹的材料必须带原题、原链接、作者与日期、正文文件路径、完整性与历史检查结果、正文哈希和原句证据、推荐理由及最强反对理由。只找到标题、短帖或摘要时继续留在自己的线索文件，不能占合格名额。

默认每组完成至少 2 条后再一次性交接，其他时间独立执行，不为普通进度、单条发现、零结果或鼓励语频繁互发消息。例外仅限真实权限阻塞、会导致覆盖或重复交付的冲突、需要另一方决策的依赖，以及最终复核退回补足。长正文放文件，消息只给路径与必要结论；接收方回读材料，消息不会自动共享全部历史。统筹先完成自己的渠道，不高频追问或轮询其他组；其后使用可用的条件等待/完成通知。任务失联或换人时，从检查点恢复，避免重复派工。消息不能扩大用户授权或替另一任务绕过权限。

统筹对准备交付的每篇回读完整正文、复查去重与五维证据，不直接接受队友的“通过”。统筹自己找到的文章，在完成后一次性交另一任务独立复核；有分歧时回到正文裁决，不靠投票或改标题放行。退回造成某组少于 2 条时，该组继续补足，不能把初筛通过数当最终交付数。每组保留独立材料文件，整批至少 6 条，其中至少 5 条文章型材料、GitHub 最多 1 条，仍由统筹统一交付。

### 只给用户一份筛好的结果

由统筹选择现有发布工具支持的一个归属（主力或主力2），统一发布一份 HTML、同序链接并负责这批反馈；其他任务不另发各自的候选页。淘汰、重复、已拒绝、撤回、未读完和仅供核验的资料留在内部，不混入用户审核页，也不通过折叠栏交给用户二次筛选。“汇总”默认汇总筛选后的结果，不等于拼接全部搜索记录。

卡片面向没有过程上下文的人，只讲原文讲了什么、为什么值得看、可以写什么；不展示内部状态码、去重术语或自评分。必要事实限制用完整、易懂的话解释，不能靠删掉警告把不合格项装成合格项。用户反馈由归属任务导入一次，统筹同步相关判断给三方；各任务不重复改共享 Skill，改规则另行按仓库版本流程执行。

## 高优先级：Ego Browser 主动检索与并行空间

Ego Browser 是高优先级的信息获取入口，主动复用现成登录态，在 X、小红书、知乎、Reddit、公众号及作者网站内搜索、阅读全文、追踪作者与引用链。不能只停留在 CLI、RSS 或聚合搜索摘要；缺 API Key 不代表缺检索能力。

用户明确允许多 space、多窗口并行探索。按平台或研究分工建立独立 space，各任务独占自己的空间并在其中使用多个标签页；不同空间可并行，同一空间的导航、点击、读取必须串行。每轮显式选择空间和目标标签，读取前核对 URL，内容为空先检查视口及加载状态。将空间 ID、平台、负责任务与断点记入私有检查点，及时关闭无用标签；仅关闭自己创建的空间，不干扰用户窗口或其他任务。

用户授权按需求大量多开：不预设 space、窗口、标签数量或探索深度上限，可以使用几十路独立浏览上下文并行，不人为限为少量标签。并发是独立空间/页面的工作，不是在同一空间同时导航，也不等于必须创建几十个模型任务。进程、标签、窗口不是同一个计数；可观测的本轮浏览器相关总进程超过 100 时，主动关闭自己创建的旧页面和已淘汰页面，保存仍有用的证据，不删除用户窗口、历史反馈或系统进程。无法准确归属进程时如实记录，用页面响应和内存压力辅助判断，不宣称“100以下一定安全”。出现实际卡死、持续超时或内存压力时及时清理并恢复工作，不把数量当稳定性保证。平台限流按平台退避，登录或验证码由用户处理，不通过多开窗口绕过。

浏览器负责取得真实材料，正文仍须经过标题预筛、完整性、去重和编辑终审。只在页面确实要求登录、验证码或当前能力无法满足时请求用户支持，不预先要求导出 Cookie 或购买接口。

## 完成契约

每批只有同时满足以下条件才算完成：

- 三任务模式每组至少 2 条、整批至少 6 条未写、未审的新材料；独立单任务常规批次至少 5 条。不得重复占用另一任务尚未交接的材料。
- 三任务模式至少 5 条文章型材料，单任务至少 4 条；GitHub 最多 1 条，只作补充。
- 每条均通过机器资格检查和证据化人工终审。
- 已用 `publish_batch.py` 完成归属登记、跨任务去重和审核页重建。
- 回复中的原文链接与 HTML 卡片数量、标题、URL、顺序完全一致。

不足时继续扩源、全文读取与筛选。单轮 0 条是正常中间状态，不得降标、拿线索凑数或要求用户再次催“继续”。用户主动暂停、取消或真实权限阻塞除外。长任务在 `.local/work/` 维护一个可续跑检查点。

## 必读与按需加载

每批开始必须读取：

1. [编辑判断模型](references/editorial-judgment.md)
2. [反馈学习协议](references/feedback-learning-protocol.md)
3. [编辑画像配置](resources/editorial_profile.json)
4. [来源配置](resources/content_curator_sources.json)
5. [加权来源组合](resources/source_portfolio.json)

边界难判时读取 [正反校准案例](references/editorial-calibration-cases.md)。扩源时读取 [来源发现清单](resources/source_discovery_playbook.md)。多平台检索读取 [Agent Reach 路由](references/agent-reach-discovery.md)。使用朱雀时读取 [朱雀说明](references/zhuque-aigc.md)。

## 决策流水线

### 1. 继承状态

- 读取 `.local/editorial_feedback.jsonl`、已交付批次和当前任务检查点。
- 按 URL、候选 ID、主题族和正文去重。
- 长期 session 记住已探索内容，同时将已看链接、正文指纹、核心问题/结论和淘汰原因写入本组私有记录；搜索前读取共享反馈与已交付记录。不能只靠对话记忆，避免压缩、重启、换 session 后失忆。相似主题只有新增事实、方法或实质结果足以构成新文章时才继续，换标题、换转载地址不算新材料。
- 三任务协作时由统筹处理共同批次；彼此独立的批次仍由各自归属任务处理。不得按下载时间猜反馈归属。

### 2. 发现与全文恢复

- 默认优先中文作者已经整理、解释和判断过的完整材料。
- 按原始发布时间从新到旧搜索，优先当天、最近几天的材料，扩大作者和渠道优先于放宽时间。仍严格执行即时事件 5 天与画像深度材料时间窗；较早深文须说明当下价值。转载时间、网页更新时间和重新上榜不能刷新旧内容的年龄，最终交付前重新检查时效。
- 英文官方资料用于事实核验，不直接占候选名额。
- 聚合页、社区、X、Reddit、GitHub 先作为线索；回到完整简体中文正文、字幕或逐字稿后才能成为候选。
- 视频或播客没有逐字稿时只保留为线索。逐字稿须去时间码、恢复标点和段落，不能把字幕墙交给用户。
- 检查原始公开页面。登录、关注、验证码或付费后才能看到剩余正文时直接淘汰；代理或缓存抓到隐藏文字不算公开完整。
- 单个来源抓取失败时记录到 `run.json` 并继续其他来源；不得把网络、鉴权或解析失败误报为“没有好材料”。所有可用路径都失败且需要新授权时，才作为真实阻塞说明。
- 每次搜索必须用 `discovery_ledger.py` 记录来源族、渠道、查询、结果数、全文数、合格数和最终入选数。安装但未实测、配置但本周未使用，都不算覆盖。
- 每周运行 `source_coverage.py`。覆盖率按高价值来源族权重计算：真实连通目标至少 85%，近 7 天实际探索目标至少 80%；未达标时优先补覆盖缺口，不得继续重复同一批网页关键词。

### 3. 资格门槛

资格问题是可确定事实，未通过即停止评估：

- AI 相关、简体中文、完整材料、来源可访问。
- 未写、未审、未被另一任务交付或预占。
- 即时事件不超过 5 天；普通深度材料遵守画像时间窗。
- GitHub 实时不少于 100 Star，且最近 7 天有与候选价值相关的实质更新。
- 不属于明确屏蔽来源、作者或精确主题状态。

### 4. 五维人工终审

先用原文标题做三态预筛：明显错位、值得读、信息不足。看标题实际承诺的题材、受众和问题；不得先改成好听标题，再替原材料补“通用价值”。标题抽象不等于没有价值，数字、人物经历也不自动淘汰。正向标题只能获得全文阅读优先级。核验标题预筛时分别检查好题保留率和坏题放行率，不能用“全部拒绝也很高”的总准确率自证有效。

原页面核对的标题存入 `source_title`，自拟切口存入 `editorial_angle`。审核卡和同序链接优先展示原题，自拟切口另列，不能用改题掩盖原文的题材和受众。

按顺序回答，不能用后项优点补救前项失败：

1. **选题吸引力**：问题、产品或矛盾本身是否值得目标读者关心？无趣产品不能靠负面批评变成好题。
2. **读者改变**：读者看完会改变哪个行动、方法或重要判断？“了解趋势”“获得启发”不算。
3. **材料增量**：原文是否提供新的事实、机制、失败、因果链或有条件的取舍？篇幅、数字、权威身份和案例数量不等于增量。
4. **二创独立性**：去掉作者身份、私人截图、企业数据和品牌素材后，能否依据公共事实与可迁移逻辑重新建立论证？
5. **长期价值**：即时性消失后是否仍值得阅读？若只靠新闻刺激，必须在即时窗口内处理。

每条通过项必须写入 `manual_editorial_review`：

```json
{
  "status": "passed",
  "topic_appeal": "为什么目标读者会关心，引用正文事实",
  "reader_change": "读者看完具体会改变什么",
  "material_increment": "原文独有的新事实、机制或取舍",
  "re_authorability": "不依赖作者专属资产仍能成立的论证",
  "durability": "为什么过了热点期仍有价值",
  "counterargument": "最强淘汰理由",
  "decision_driver": "最终放行所依据的决定性证据"
}
```

缺任一字段、只写抽象赞美、没有正文证据，均不得发布。

终审同时填写 `source_sha256`（当前完整正文的 SHA-256）和 `source_anchors`（至少分别给 material_increment、re_authorability 两项提供 `{dimension, quote}`）。quote 必须是正文中可定位的原句；正文变化后重审。程序只验证引用真实存在，不证明解释正确，仍须检查原句能否支撑推荐理由。

### 5. 风险信号的使用方式

AI 味、新闻腔、宣传、第一人称、技术词、宏观词、图片多、QA、访谈、论文或垂直行业只是调查信号，不是类别禁令。必须继续问它是否造成了实质缺陷：

- 信息密度是否被模板结构取代？
- 读者是否必须掌握专业背景才能理解主线？
- 论证是否依赖私人素材或单一公司环境？
- 作者是否只有翻译、拼接和转述，没有自己的判断？
- 产品与问题是否缺乏真实需求和吸引力？

不要因为命中一个词直接淘汰，也不要另拟漂亮切口替原材料补价值。边界判断使用成对案例校准。

### 6. 报告、发布与反馈

运行：

```bash
python3 -m pip install -r scripts/requirements.txt
python3 scripts/scrape_aihot.py
python3 scripts/publish_batch.py topics/<批次ID> --owner 主力
```

另一任务使用 `--owner 主力2`。正常报告不得包含资格不合格项；`--include-rejected` 只用于调试。

用户审核后，先核对批次 ID、归属、候选 ID、标题、链接及顺序，再导入：

```bash
python3 scripts/import_feedback.py /path/to/selection_feedback.json --expected-batch <批次ID> --owner 主力
```

导入成功并回读后才删除下载 JSON；失败必须保留。按钮状态决定当前候选结果，备注负责学习原因。按钮与备注冲突、只有空备注否决或含义不清时，按 [反馈学习协议](references/feedback-learning-protocol.md) 处理，禁止猜测。

优化判断前先运行 `python3 scripts/feedback_audit.py`，确认反馈批次、数量、空备注、重复候选和待解释冲突均被覆盖。审计只输出安全摘要，不自动把备注变成规则。

## 运行边界

- 不撰写文章正文；选题确认后交给 `stephen-writing-skill`。
- 不提交 `.local/`、`.config/`、`topics/`、反馈、密钥、Cookie 或登录态。
- 本项目不调用 OpenCLI，避免接管用户 Chrome；动态或登录页面使用 Ego Browser 隔离空间。
- Ego Browser 的现成登录态是正式检索途径。CLI 缺凭据时先实测站内搜索与正文，不得直接要求导出 Cookie 或购买接口；按操作记录成功与失败。
- 朱雀和模型复排都是辅助证据。未配置或失败表示未知，不能证明文章由人创作。
- 自动分数名为 `discovery_score`，只负责发现排序。最终发布必须通过证据化人工终审。
- 单批反馈不得把读者兴趣、题材偏好、技术难度、行业价值或二创难度升级为客观硬失败。它们只能成为风险信号或待验证假设；只有语言、公开完整性、时效、精确历史状态、来源安全和可核验平台门槛等事实可以硬拦截。

## 修改后的验证

```bash
.venv/bin/python3 -m unittest discover -s tests -v
uv run --with pyyaml python /Users/a1-6/.codex/skills/.system/skill-creator/scripts/quick_validate.py .
git diff --check
```

修改后只提交本次相关文件，推送 `origin/main`，并回读确认本地 `HEAD`、`origin/main` 与远程 `main` 一致。禁止 force push。
